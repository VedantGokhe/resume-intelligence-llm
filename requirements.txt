# Resume Intelligence LLM — Python dependencies
# Install: pip install -r requirements.txt

# Core model / training
transformers>=4.40.0
peft>=0.10.0
trl>=0.8.6
bitsandbytes>=0.43.0
accelerate>=0.28.0
datasets>=2.18.0
sentencepiece
scipy
einops

# Inference API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Schema / validation
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Evaluation helpers
scikit-learn>=1.4.0
rapidfuzz>=3.6.0        # fuzzy matching for hallucination detection
jsonschema>=4.21.0

# Dev / testing
pytest>=8.0.0
black>=24.0.0
isort>=5.13.0

# Optional: Unsloth (2x faster QLoRA on T4) — install separately in Colab
# pip install unsloth
